export interface InitialFormStyle {
  Header: {
    fontFamily: string;
    fontSize: string;
  };
  Question: {
    fontFamily: string;
    fontSize: string;
  };
  Answer: {
    fontFamily: string;
    fontSize: string;
  };
  primaryColor: string;
  secondaryColor: string;
}
