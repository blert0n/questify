query Responses($formId: String!) {
  Form_by_pk(id: $formId) {
    id
    responses
    FormItems(order_by: { order: asc }) {
      id
      name
      Answers_aggregate {
        aggregate {
          count
        }
      }
      type
      Answers {
        id
        value
      }
    }
  }
}
